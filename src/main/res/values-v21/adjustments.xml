<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="BaseTheme">

        <!-- Hides collapsed toolbar but overlaps drawer. -->
        <!-- <item name="android:statusBarColor">@color/ez_primary_dark</item> -->

        <!-- Drawer visible under status bar BUT toolbar not hidden when collapsed. -->
        <item name="android:statusBarColor">@android:color/transparent</item>

        <!--
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowActionBarOverlay">true</item>-->
        <item name="android:windowContentOverlay">@null</item>
        <!--
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowTranslucentStatus" tools:targetApi="21">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds" tools:targetApi="21">true</item>
        -->

        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <item name="android:windowSharedElementEnterTransition">@android:transition/move</item>
        <item name="android:windowSharedElementExitTransition">@android:transition/move</item>
    </style>

    <style name="NavDrawerItem" parent="BaseNavDrawerItem">
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>

    <style name="NavDrawerSubheader" parent="BaseNavDrawerSubheader">
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>

    <!-- Used to keep drawer content below status bar. -->
    <dimen name="status_bar_height">24dp</dimen>

</resources>
